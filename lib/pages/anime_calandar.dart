import "package:anime_world/colors.dart";
import "package:flutter/material.dart";

class CalandarTab extends StatelessWidget {
  const CalandarTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          color: MyColor.secondary,
          ),
      ),
    );
  }
}